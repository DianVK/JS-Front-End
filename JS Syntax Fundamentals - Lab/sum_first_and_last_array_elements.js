function sumFirstLastElement(elements) {
    firstElement = elements[0]
    lastElement = elements[elements.length - 1]
    console.log(firstElement + lastElement)
}

sumFirstLastElement([20, 30, 40])